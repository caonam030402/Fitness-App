import 'package:flutter/material.dart';

class AppShadows {
  static const shadowCard = BoxShadow(
    color: Color.fromARGB(95, 229, 231, 241),
    spreadRadius: 4,
    blurRadius: 1,
    offset: Offset(0, 2), // changes position of shadow
  );
}
