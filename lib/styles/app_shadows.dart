import 'package:flutter/material.dart';

class AppShadows {
  static const shadowCard = BoxShadow(
    color: Color.fromARGB(95, 179, 189, 250),
    spreadRadius: 0,
    blurRadius: 1,
    offset: Offset(0, 1), // changes position of shadow
  );
}
